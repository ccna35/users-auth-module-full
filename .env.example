PORT=3000
DATABASE_URL=mysql://appuser:apppass@localhost:3306/appdb

# JWT access (short-lived)
JWT_ACCESS_SECRET=replace-me-strong-secret
ACCESS_TOKEN_TTL=15m

# Opaque refresh tokens (DB-stored, hashed)
REFRESH_TOKEN_TTL=7d

# Password reset token TTL
RESET_TOKEN_TTL=1h
